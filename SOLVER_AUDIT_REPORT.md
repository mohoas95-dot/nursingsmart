# گزارش Audit معماری و کد Solver — NursingSmart

> **نوع سند:** گزارش بازبینی (بدون هیچ تغییر در کد/دیتابیس/تنظیمات)
> **تاریخ:** ۱۴۰۵/۰۵/۲۲ (مطابق ساعت کاربر)
> **شاخه:** `arena/019ff9e5-nursingsmart`
> **دامنه:** تمام ماژول‌های مرتبط با Solver / زمان‌بندی / سناریو / امتیازدهی / قفل‌ها / تست‌ها

**قراردادهای این گزارش:**

- هر ادعا یا مستند به کد است، یا صریحاً `UNKNOWN / NEEDS VERIFICATION` نوشته می‌شود.
- هر قابلیت با یکی از سه برچسب طبقه‌بندی می‌شود:
  - **A)** وجود دارد و درست کار می‌کند
  - **B)** وجود دارد ولی ناقص یا اشتباه است
  - **C)** اصلاً وجود ندارد
- هیچ چیز تغییر داده نشده است. این گزارش فقط «توصیف وضعیت موجود» است.

---

## 1. EXECUTIVE SUMMARY (حداکثر ۱۰ خط)

1. آنچه «Solver» نامیده می‌شود در واقع یک **سازندهٔ حریصانهٔ قطعی** (greedy, day-by-day constructor) است، نه یک Solver بهینه‌سازی (هیچ LP/MIP/CP یا جستجوی بهینه در پروژه نیست).
2. هیچ «تابع هدف واحد» وجود ندارد؛ کیفیت در **comparatorهای sort با اعداد جادویی** (۲۰۰۰۰۰، ۱۰۰۰۰۰، ۵۰۰۰۰، …) و ترتیب اجرای پروسیجرها پخش شده است.
3. در لایهٔ سناریو، معماری از «امتیاز تلفیقی» به **«مبنامحور» (Baseline-Oriented)** تغییر کرده و در عمل `totalScore` برابر **درصد شباهت به برنامهٔ مبنا** است؛ درخواست پرسنل فقط tiebreaker رتبهٔ ۳ و عدالت کلاً از تصمیم حذف شده.
4. بزرگ‌ترین مشکل: **Baseline (Working Roster) از «نقطهٔ شروع» به «قفس» تبدیل شده** — هدفِ بالفعلِ سیستم «کمینه‌سازی تغییر نسبت به مبنا» است، نه «بهترین برنامهٔ ممکن».
5. مفهوم **Degree of Freedom / Flexibility** (گرادیانی بین Completely Locked تا Flexible) **وجود ندارد**؛ فقط قفل دودویی (lockedRows / person.locked) به‌علاوهٔ چند پرچم پراکنده است.
6. قوانین Hard/Soft به‌صورت سیستماتیک از هم جدا نشده‌اند؛ طبقه‌بندی «سطح A» فقط **بر اساس پیشوند رشتهٔ هشدار** انجام می‌شود و با منطق واقعی Solver هم‌خوانی ندارد.
7. `reconcileStaffingCoverage` (جبران پوشش) که **بعد از Solver و روی هر تغییر برنامه اجرا می‌شود**، در مسیر پر کردن کمبود، درخواست‌های Hard OFF / shift / avoid_shift را **نادیده می‌گیرد** → می‌تواند تصمیم درست Solver را خراب کند.
8. «روز بدون درخواست = پیش‌فرض استراحت» و «جایگزینی خودکار» به‌صورت **کد مرده** در `lib/balanceChecker.ts` هست ولی به هیچ مسیر اجرایی وصل نیست.
9. تست‌ها قوانین (سقف، پوشش، تمایز سناریو) را می‌سنجند، نه **کیفیت برنامه، رضایت درخواست، ترجیح استراحت، یا بن‌بست**؛ و اسکریپت `npm test` فقط تست‌های سطح بالا را اجرا می‌کند (تست‌های `tests/domain/` خارج از گلوب هستند).
10. هیچ سناریوی واقعی بیمارستانی و هیچ تستی برای «قفل چند ردیف → آیا بقیه برنامه درست می‌شود؟» وجود ندارد.

---

## 2. WHAT IS GOOD (بخش‌هایی که نباید بی‌دلیل تغییر کنند)

این بخش‌ها سالم و ارزشمندند و باید در هر Refactor حفظ شوند:

| مورد | وضعیت | توضیح |
|---|---|---|
| `domain/` لایهٔ خالص و بدون I/O | A | تقریباً تمام ماژول‌های domain توابع خالص و قابل تست دارند (smart-rules, request-scope-matcher, shift-edit-guards, duty-hours-calculator, schedule-operations, alert-lifecycle). |
| قانون سقف ۵ شیفت متوالی (اسلات‌محور و وزن‌دار) | A | `domain/scheduling/smart-rules.ts` یک مدل یکپارچه و مستقل از شیفت دارد (M=۱، E=۱، N=۲، سقف ۵). این بهترین بخش کد از نظر طراحی است؛ مستند و تست‌شده. |
| تفکیک مؤلفه‌ای پوشش (M/E/N) | A | `staffing-coverage.ts` تغییر یک مؤلفه را بدون تخریب مؤلفهٔ دیگر انجام می‌دهد (`setShiftPeriod`, `SHIFT_COMPONENTS`). |
| تشخیص شیفت تک‌تک و احترام به تگ روتین | A | `isIsolatedSingleShiftAt`, `wouldCreateIsolatedShift`, `ROUTINE_PREFERRED_SHIFTS`, `ROUTINE_PERIOD_ACCESS` منطق تمیز و تست‌شده دارند. |
| مرخصی روز تعطیل (LH) با اعتبار دقیق ۷ ساعت | A | قانون در smart-rules و solver و گزارش‌ها یکدست پیاده شده و تست دارد. |
| محاسبات ساعت موظفی و سنوات | A | `calculateAutoDutyHours`, `getSeniorityHours`, `getLeaveHours`, `duty-hours-calculator.ts` قطعی و سازگارند. |
| الگوی Facade با تزریق وابستگی | A | `shift-write-facade.ts` جداسازی validation→domain→persistence و قابلیت تست دارد. |
| قراردادهای خواندنی (Readonly) | A | امضای توابع domain/solver عمدتاً Readonly شده و از mutation ورودی جلوگیری می‌کند. |
| حفاظت از سلول‌های ویرایش دستی سرپرستار | B | ایدهٔ `protectedCells` درست است، ولی اجرای آن ناقص است (پایین را ببینید: reconcile آن را در مسیر «کمبود» رعایت نمی‌کند). |
| ثبت رویدادها و گزارش اجرای Solver | A | `solver-report.ts` و event log رویدادهای اجرا را با ساختار ثبت می‌کنند. |

**نتیجه:** هستهٔ «قوانین شیفت» (smart-rules + staffing-coverage + scope-matcher) باکیفیت است؛ مشکل در **لایهٔ تصمیم/هدف** (solver.ts) و **لایهٔ سناریو/رتبه‌بندی** (scenarioGenerator + objective) است.

---

## 3. WHAT IS MISSING (قابلیت‌های لازم که وجود ندارند)

| # | قابلیت لازم برای فلسفهٔ محصول | وضعیت | شواهد |
|---|---|---|---|
| 1 | **Degree of Freedom / Flexibility** به‌ازای هر فرد یا هر تصمیم | **C** | مدل فقط `locked?: boolean` (در Personnel) و `lockedRows: string[]` (در Schedule) دارد. هیچ فیلد عددی/رتبه‌ای مثل `flexibility` یا سطح اولویت وجود ندارد. |
| 2 | **تابع هدف واحد و صریح** | **C** | کیفیت در comparatorهای sort پراکنده است (solver.ts). دو «تابع هدف» مجزا وجود دارد که با هم نمی‌خوانند (scoring.ts در برابر objective.ts). |
| 3 | **هزینهٔ قابل‌مقایسه برای نقض هر ترجیح** | **C** | تنها «وزن»ها: `requestDayWeight` (isEssential=1.25، hard OFF=+0.15، essential leave=+0.1) و اعداد جادویی در sort. هیچ واحد مشترکی برای مقایسهٔ «نقض Request در برابر نقض Rest در برابر Productivity» نیست. |
| 4 | **Default Preference for Rest / Minimum Intervention** | **C (کد مرده)** | `applyDefaultOffRule` در `lib/balanceChecker.ts` نوشته شده ولی هیچ‌جا صدا زده نمی‌شود. Solver فعال، روز بدون درخواست را با روز دارای درخواست یکسان می‌بیند. |
| 5 | **مدل هزینهٔ «اثر شیفت سنگین بر روز بعد» به‌صورت گرادیانی** | **C (فقط باینری)** | اثر سنگینی فقط به دو شکل باینری است: ممنوعیت‌های سخت (N بعد از M، heavy بعد از heavy، …) و وزن شب=۲ در سقف متوالی. «هزینهٔ بیشتر ولی مجاز» به‌صورت عددی وجود ندارد. |
| 6 | **Productivity به‌عنوان قاعدهٔ صریح با اولویت معلوم** | **B** | در Solver فقط یک tiebreaker نرم است (`qualifiesForProdSecRow`). هیچ قاعدهٔ سخت یا هزینهٔ مشخصی برای حداقل M/E/N نیست. |
| 7 | **Relax منطقی ترجیحات در Fallback (دانستن کم‌ارزش‌ترین ترجیح)** | **C** | Fallback‌ها فقط «Soft OFF در آخر» و «ناسازگاری روتین در آخر» را می‌دانند؛ هیچ ترتیب Relax عمومی یا ارزیابی هزینه‌ای وجود ندارد. |
| 8 | **توضیح «کدام محدودیت بیشترین فشار را ساخت»** | **C** | خروجی فقط هشدارهای رشته‌ای است؛ هیچ گزارش ساخت‌یافتهٔ «فشار» یا علت اصلی (bottleneck) تولید نمی‌شود. |
| 9 | **تضمین عدم نقض Hard Constraint پس از Post-processing** | **C** | reconcile می‌تواند Hard OFF را در مسیر کمبود نقض کند و فقط «Mismatched Request» (غیربحرانی) می‌سازد. |
| 10 | **سناریوهای واقعی بیمارستانی در تست** | **C** | تست‌ها از fixtureهای مصنوعی ۲–۴ نفره استفاده می‌کنند. |
| 11 | **انتخاب «بهترین برنامهٔ ممکن» به‌جای «اولین برنامهٔ قابل‌قبول»** | **C** | سازندهٔ حریصانه اولین گزینهٔ سازگار را انتخاب می‌کند؛ هیچ جستجو/بهبودی نیست. |

---

## 4. WHAT IS WRONG (وجود دارد ولی رفتارش اشتباه است)

### 4.1 Baseline عملاً «قفس» شده است — B (اشتباه معماری، نه باگ تصادفی)
- در `lib/scenarioGenerator.ts` → `scoreCandidate`: `scored.totalScore = objective.similarityPercent`.
- در `domain/scenarios/objective.ts` → `compareByObjective`: رتبه‌بندی = شباهت (نزولی) ← تعداد هشدار غیربحرانی ← رضایت درخواست.
- در `docs/SCENARIO_REFACTOR.md` صراحتاً نوشته شده: «عدالت دیگر معیار انتخاب یا رتبه‌بندی نیست» و «درخواست‌ها فقط tiebreaker پس‌زمینه».
- یعنی هدفِ واقعیِ فعلی سیستم = «نزدیک‌ترین برنامه به Working Roster بدون هشدار سطح A». این دقیقاً همان «قفس» است که در پرامپت نگرانش بودید. **برنامهٔ مبنا هم نقطهٔ شروع است و هم معیار نهایی؛ هر انحراف از آن جریمه است.**

### 4.2 دو Objective متناقض — B
- `lib/scoring.ts` هنوز `SCENARIO_WEIGHTS` (REQUESTS: ۷۰/۲۰/۱۰، FAIRNESS: ۲۰/۷۰/۱۰، MIXED: ۴۵/۴۵/۱۰) و `weightedTotal` را محاسبه می‌کند.
- اما مسیر اجرایی (`scenarioGenerator`) این مقدار را با `similarityPercent` بازنویسی می‌کند.
- `weightedTotal` فقط در مسیر قدیمی `hydrateStoredScenario` (برای سناریوهای ذخیره‌شدهٔ قدیمی بدون `metrics`) مصرف می‌شود. نتیجه: دو سیستم امتیازدهی که خروجی‌شان معنی متفاوت دارد و هر دو زنده‌اند.

### 4.3 `reconcileStaffingCoverage` می‌تواند Hard Constraint را بشکند — B (جدّی)
- در مسیر «کمبود» (`assigned < required`)، فیلتر `available` فقط این‌ها را حذف می‌کند: ردیف قفل‌شده، سلول محافظت‌شده، مرخصی، و «از قبل پوشش دارد».
- **درخواست Hard OFF، درخواست shift متفاوت، و avoid_shift در این فیلتر بررسی نمی‌شوند** (فقط Soft OFF در sort جریمهٔ +۸۰ می‌گیرد).
- بنابراین reconcile می‌تواند روی روزِ یک Hard OFF شیفت بگذارد؛ و `verifyCoverageAndLeaders` فقط «Mismatched Request» تولید می‌کند که در `HARD_WARNING_PREFIXES` نیست → به‌عنوان «سطح A» هم شناخته نمی‌شود.
- در مسیر «مازاد» (`assigned > required`)، حذف مؤلفه بدون بررسی درخواست shift انجام می‌شود → می‌تواند رضایت درخواست را کم کند.

### 4.4 دو سازوکار قفل ناهمگن — B
- `personnel.locked` (بولی روی نفر) و `lockedRows` (آرایه روی سند برنامه) دو مفهوم موازی‌اند.
- `solveNursingSchedule` فقط `p.active` را فیلتر می‌کند و **`p.locked` را نادیده می‌گیرد**؛ یعنی نفرِ `locked=true` در Solver عادی مثل بقیه چیده می‌شود و فقط بعداً در `mergeOptimizerAssignments`/`reconcile` از مبنا حفظ می‌شود. این حفاظت «پس‌اَنداز» می‌تواند کمبود/مازاد جدید بسازد (به همین دلیل Facade دوباره reconcile می‌زند).
- `solveWithPriority` برعکس، `!p.locked` را فیلتر می‌کند. دو تابع اصلی Solver دربارهٔ قفل نفر رفتار متفاوتی دارند.

### 4.5 طبقه‌بندی «سطح A» مبتنی بر متن رشته است — B
- `HARD_WARNING_PREFIXES` (در scoring) بر اساس **پیشوند رشتهٔ فارسی** هشدار است: `Coverage Shortage:`, `Overstaffing:`, `Missing Shift Leader:`, `Max Consecutive:`, `Mandatory Rest:`.
- این یعنی «Hard بودن» یک مفهوم ثانویهٔ نمایشی است، نه خاصیت ساخت‌یافتهٔ مدل. یک قانون واقعاً سخت که هشدارش با این پیشوندها شروع نشود (مثل نقض Hard OFF توسط reconcile) در سیستم «نرم» تلقی می‌شود. **منبع حقیقت Hard/Soft باید مدل داده باشد، نه متن هشدار.**

### 4.6 «تعمیر هشدار» با regex روی متن فارسی — B (شکننده)
- `scenarioGenerator.generateCriticalRepairEdits` برای فهمیدن روز/شیفت/نفر، متن هشدار فارسی را regex می‌زند (`/روز (\d+)/`, `/شیفت ([A-Z]+)/`, `/نوبت (صبح|عصر|شب)/`, جستجوی نام کامل نفر).
- ساختار داده در تولید هشدار از بین رفته و دوباره از متن بازسازی می‌شود. هر تغییر جزئی در wording، این تعمیر را بی‌صدا خراب می‌کند. این دقیقاً همان ضدالگوی «نشت ساختار به رشته» است.

### 4.7 `handleRunOptimizer` دیگر برنامه تولید نمی‌کند — B (تغییر معنای دکمه)
- دکمهٔ «بازتولید هوشمند» حالا فقط `generateAndScoreScenariosWithProgress` را صدا می‌زند (تولید سناریوهای مبنامحور)؛ `solveNursingSchedule`/`solveWithPriority` مستقیماً در این مسیر نیستند.
- `solveWithPriority` عملاً فقط در `handleApplyScenario` (mockSolver) برای محاسبهٔ warningها صدا می‌شود؛ **assignments از خود سناریو می‌آید**. یعنی «موتور اولویت‌بندی سه‌سطحی» دیگر هیچ برنامه‌ای تولید نمی‌کند؛ یک مسیر مرده/نیمه‌مرده است.

### 4.8 `smartSuggestion.ts` بسیار سطحی است — B
- پیشنهادها فقط با «یک درجه پایین‌آوردن» شیفت (MEN→ME، MN→M، …) و تطبیق زیررشتهٔ «شیفت X تخصیص یافته» ساخته می‌شوند؛ هیچ ارزیابی اثر روی Coverage یا Request انجام نمی‌دهد.

---

## 5. WHAT IS TOO COMPLEX (تکراری، متناقض، بیش‌ازحد پیچیده)

1. **`lib/solver.ts` (۱۹۴۹ خط)** — پنج مرحلهٔ ساخت + fallback + ترمیم شیفت تک + post-process + reconcile + verify در یک فایل. منطق تصمیم (comparatorها) در ۴–۵ نقطهٔ مختلف تکرار شده است.
2. **تکرار `isRequestedForThisShift` / `matchesM/E/N`** — الگوی «آیا pref با shiftChar سازگار است» ده‌ها بار با copy/paste تکرار شده است (در sort مازاد، فیلتر supervisor/staff، فیلتر routine، comparator اصلی).
3. **تکرار full scope-matching** — منطق `scope` (all/even/odd/weekdays/range/custom_days) در سه جا تکرار شده است: `solver.ts` (دو بار: compile و verify)، `request-scope-matcher.ts` (نسخهٔ درست)، `staffing-coverage.ts` (نسخهٔ ساده‌شده). سه پیاده‌سازی از یک مفهوم.
4. **سه سازوکار جبران کمبود/مازاد** — (a) forceAvailable در solver، (b) `reconcileStaffingCoverage`، (c) `solveWithPriority`. هر سه با قوانین کمی متفاوت دربارهٔ Hard OFF / leave / routine. رفتار نهایی وابسته به این است که کدام‌یک آخر اجرا شود.
5. **دو سیستم Lock** (person.locked در برابر lockedRows) + **دو سیستم Finalize** (finalized/finalizedNurses/finalizedAssistants در برابر finalizedNursesMonths/finalizedAssistantsMonths) + **دو سیستم امتیاز** (scoring در برابر objective) + **دو سیستم Request‌priority** (isEssential در برابر offHardness). هر مفهوم دوبار مدل شده است.
6. **`app/page.tsx` (۹۵۹۵ خط)** همچنان ارکستراتور همه‌چیز است: ذخیره‌سازی، ETag/تداخل، Solver، سناریو، هشدار، چاپ، احراز هویت. منطق Solver به‌طور کامل از UI جدا نیست.
7. **`lib/scoring-stub.ts`** فایل stub مرده است و `lib/balanceChecker.ts` هم عملاً مرده است؛ `lib/scoring.ts` نیز بخش عظیمی از آن (`weightedTotal`, `evaluateScenarioSchedule`, `evaluateSchedule`, `calculateScenarioDifferencePercent`) در مسیر فعال استفادهٔ چندانی ندارد.
8. **اثر ذخیره‌سازی زنجیره‌ای** — هر تغییر کوچک → `useEffect` پیوسته → reconcile (تا ۳ پاس) → verify → save → sync. این زنجیرهٔ خودکار باعث می‌شود «چه کسی واقعاً برنامه را تغییر داد» غیرقابل ردیابی شود.

---

## 6. CURRENT DECISION HIERARCHY (آنچه کد واقعاً انجام می‌دهد — نه آنچه ادعا می‌شود)

### مسیر ۱: ساخت برنامهٔ مبنا — `solveNursingSchedule` (ترتیب واقعی اجرا)

1. **مرخصی** (هر روز leave → `L1..Ln`؛ تعطیل رسمی → `LH`). عملاً غیرقابل نقض (به‌جز مسیرهای اضطراری).
2. **الگو (pattern)** — گام‌های pattern روی کل ماه پخش می‌شود (روی مرخصی نمی‌نشیند).
3. **درخواست‌های صریح OFF و shift** — روی سلول‌ها اعمال می‌شود (درخواست shift عیناً می‌نشیند؛ اگر چند درخواست روی یک روز، `isEssential` برنده می‌شود).
4. **سرپرستار و Staff** — به‌طور پیش‌فرض `M` (تعطیل = OFF؛ با درخواست صریح یا avoid M عوض می‌شود). یعنی «فیکس صبح» سرپرستار/استاف به‌صورت hard در این‌جاست.
5. **پر کردن روزبه‌روز تقاضا** — ابتدا کمک‌بهیار (M سپس E سپس N)، سپس پرستار (M، E، N). برای هر gap:
   - فیلتر `available`: مرخصی/روتین ناسازگار/درخواست مخالف/Hard OFF/ممنوعیت‌های سخت/سقف متوالی/heavy-پشت-heavy/سقف ۲۴۰ ساعت اضافه‌کار/حداکثر ۲ شیفت «خارج از روتین».
   - انتخاب نفر با `sort` (ترتیب اولویت واقعی): penalty عددی (جادویی) ← رعایت حداقل M/E/N بهره‌وری ← جریمهٔ «دارای روتین ولی امروز درخواست نیست» ← نزدیکی به ساعت موظفی ← نوع استخدام (وظیفه<قراردادی<رسمی<اضافه‌کار).
6. **مسیر اضطراری (forceAvailable)** — اگر gap ماند: Hard OFF و leave ضروری همچنان حفظ؛ Soft OFF در انتهای صف؛ ناسازگاری روتین در انتهای صف؛ سقف متوالی در انتهای صف. اگر باز ماند → هشدار `Coverage Shortage`.
7. **ترمیم شیفت تک‌تک** — شیفت ایزوله به نفر دیگر منتقل می‌شود.
8. **Post-process** — حذف OFF بعد از مرخصی؛ شکستن >۳ OFF متوالی (با M/E).
9. **`reconcileStaffingCoverage`** — شمارش دقیق M/E/N به‌ازای هر روز/گروه (با قوانین فوق‌الذکر).
10. **`verifyCoverageAndLeaders`** — تولید هشدارها و انتخاب سرشیفت.

### مسیر ۲: تولید/رتبه‌بندی سناریو — `scenarioGenerator` (آنچه دکمهٔ بازتولید اجرا می‌کند)

1. **مبنا** = برنامهٔ فعلی (`currentAssignments`) → verify.
2. تولید نامزدها: row-swapهای حفظ‌کنندهٔ پوشش (۲/۳ بذرها) + نامزد درخواست‌محور (۱/۳ بذرها).
3. `repairCriticalAlerts`: regex روی هشدارها → ویرایش سلول هدفمند → تا ۲۴ گام.
4. `applyQualityFilter`: حذف نامزد دارای سطح A، یا فاصله > ۳۵٪ از مبنا، یا فاصله < ۳٪ (کپی مبنا).
5. `selectTopScenarios` با `compareByObjective`: **شباهت به مبنا (نزولی) ← هشدار غیربحرانی کمتر ← رضایت درخواست بیشتر**.
6. اگر هیچ نامزدی نماند → ۰ سناریو + دلیل در `generationLog`.

### مسیر ۳: اعمال سناریو — `handleApplyScenario`

1. assignments از سناریو (نه از Solver)؛ warnings از `solveWithPriority` (که روی مبنا اجرا می‌شود).
2. `runOptimizerFacade`: merge (حفظ lockedRows) ← **reconcile دوباره** ← verify ← ذخیره و `finalizedNurses/Assistants=true`.

### مسیر ۴: هر تغییر (ویرایش دستی / ذخیره / تعویض ماه)

- `useEffect` پیوسته در page.tsx: هر تغییر assignments → reconcile (تا ۳ پاس) روی **هر دو گروه** → verify → ذخیره. این مسیر **بعد از** همهٔ مسیرهای بالا هم اجرا می‌شود و آخرین حرف را می‌زند.

> **جمع‌بندی سلسله‌مراتب واقعی:**
> `Leave > Pattern > Explicit Request > Supervisor/Staff-M > Coverage(پرکردن حریصانه با comparator) > (ترمیم‌ها) > reconcile > verify`.
> در لایهٔ سناریو: `بدون سطح A > قفل‌ها > شباهت به مبنا > هشدار کمتر > رضایت درخواست`.
> **هیچ‌کدام از این دو سلسله‌مراتب در یک مکان واحد و صریح تعریف نشده‌اند؛ هر دو از ترتیب کد و comparatorها استنتاج می‌شوند.**

---

## 7. DESIRED DECISION HIERARCHY (پیشنهاد بر اساس مشخصات محصول)

این فقط **پیشنهاد ترتیب** است (بدون کد):

1. **Hard Constraints (هرگز نشکن):** پوشش الزامی بخش (با امکان اعلام غیرممکن‌بودن)، قفل کامل، مرخصی ضروری، سقف متوالی، ممنوعیت‌های سخت شیفت متوالی، قوانین قانونی (بهره‌وری/اضافه‌کار سقف‌دار).
2. **حفظ تصمیمات قطعی سرپرستار:** سلول‌های ویرایش‌دستی + قفل‌های قوی (سطح Strongly Protected و بالاتر).
3. **نیاز واقعی بخش (Coverage)** در حد لازم — ولی نه با خرد کردن ترجیح‌ها به‌صورت کور.
4. **استراحت منطقی و اثر شیفت سنگین بر روز بعد** (هزینهٔ گرادیانی، نه باینری).
5. **Request Satisfaction به‌عنوان Objective اصلی رتبهٔ بالا** (با وزن بر اساس اولویت درخواست و درجهٔ آزادی نفر)، نه tiebreaker رتبهٔ ۳.
6. **بهره‌وری** به‌صورت Soft با هزینهٔ مشخص، رقابت شفاف با Request (کسی که حداقل M/E/N را ندارد در اولویت بالاتر قرار گیرد، ولی حق ندارد درخواست منطقی را خراب کند).
7. **عدالت** به‌عنوان Soft با وزن واقعی (نه حذف‌شده).
8. **شباهت به Baseline** فقط به‌عنوان Soft نرمال‌سازی/ترجیح «حداقل مداخله» با وزن کم (نه معیار اصلی).
9. **در صورت عدم امکان ایده‌آل:** Relax ترجیحات به ترتیب کم‌اهمیت‌ترین (با هزینهٔ قابل‌مقایسه) + گزارش «کدام محدودیت فشار اصلی بود».

> نکته: در طراحی فعلی، جای بند ۵ و ۸ **برعکس** است (شباهت به مبنا رتبهٔ ۱ و درخواست رتبهٔ ۳ است).

---

## 8. OBJECTIVE ANALYSIS

### 8.1 آنچه در Objective‌های موجود هست

| Objective | اجزاء | وزن | کجا مصرف می‌شود |
|---|---|---|---|
| `SCENARIO_WEIGHTS` (scoring.ts) | request / fairness / optimization | REQUESTS 70/20/10، FAIRNESS 20/70/10، MIXED 45/45/10 | تقریباً هیچ‌جا در مسیر فعال؛ فقط legacy hydration |
| `evaluateBaselineObjective` (objective.ts) | criticalResolved، locksPreserved، similarityPercent، requestSatisfactionPercent | شباهت = کلید رتبه؛ درخواست = tiebreaker | مسیر فعال رتبه‌بندی سناریو |
| comparatorهای داخلی solver.ts | penalty جادویی، بهره‌وری، روتین، کسری موظفی، نوع استخدام | اعداد جادویی غیرقابل‌مقایسه | انتخاب نفر در پر کردن روزبه‌روز |

### 8.2 مشکلات وزن‌دهی

- **Baseline Similarity بیش از حد قدرتمند است (اثبات‌شده):** `scored.totalScore = similarityPercent` و رتبه‌بندی نهایی = شباهت. هر سلولِ تغییر‌یافته، به‌طور یکسان جریمه است؛ «تغییر برای رفع یک درخواست منطقی» و «تغییر بی‌دلیل» یکسان تنبیه می‌شوند.
- **Request Satisfaction بیش از حد ضعیف است (اثبات‌شده):** فقط tiebreaker رتبهٔ ۳؛ در UI هم نمایش داده نمی‌شود (طبق docs). با هدف محصول («MAXIMUM POSSIBLE REQUEST SATISFACTION» و «Request نباید tiebreaker کم‌اهمیت باشد») در تضاد مستقیم است.
- **Fairness تعریف‌اش در scoring وجود دارد ولی در تصمیم استفاده نمی‌شود:** `calculateFairnessScore` محاسبه می‌شود ولی «هرگز در انتخاب/رتبه‌بندی یا UI» به کار نمی‌رود (نقل‌قول مستقیم از docs). پس Fairness در معماری فعلی عملاً **غایب** است.
- **Rest و Shift Quality در Objective نیستند:** هیچ ترمی برای «کیفیت استراحت»، «اثر سنگینی بر روز بعد» (غیر از سقف دودویی)، یا «کیفیت شیفت» در هیچ Objective وجود ندارد.
- **دو Objective ناسازگار:** عدد `weightedTotal` و عدد `similarityPercent` دو «امتیاز کل» متفاوت تولید می‌کنند که هر دو هنوز در کد زنده‌اند و مصرف‌کننده‌های متفاوتی دارند.
- **Objective فعلی با هدف محصول نمی‌خواند:** هدف محصول «مثل سرپرستار باتجربه» و «رضایت حداکثری با حفظ قوانین سخت» است؛ Objective فعلی «حداقل انحراف از مبنا» است.

---

## 9. LOCK / FLEXIBILITY ANALYSIS

### وضعیت فعلی (فهرست کامل)

| مکانیزم | سطح | کجا اعمال می‌شود |
|---|---|---|
| `personnel.locked` (بولی) | باینری | فقط `solveWithPriority` و `reconcile`؛ **در `solveNursingSchedule` نادیده گرفته می‌شود** |
| `lockedRows: string[]` (سند برنامه) | باینری | merge/reconcile/UI |
| `finalizedNursesMonths` / `finalizedAssistantsMonths` | باینری (گروهی) | guardها و saveState |
| `finalized/finalizedNurses/finalizedAssistants` روی سند | باینری (گروهی) | ذخیره‌سازی و sync |
| `isEssential` روی درخواست | باینری (اولویت بالا) | فقط برنده شدن در تعارض روز + وزن ۱.۲۵ در امتیاز |
| `offHardness: 'hard' | 'soft'` | دو سطح (فقط برای OFF) | Solver، solveWithPriority، reconcile (فقط soft جریمه می‌گیرد) |
| `workRoutine` تگ | ۳ سطح (morning/evening_night/long) | محدودسازی دورهٔ کاری نفر |
| `protectedCells` | باینری (سلول) | reconcile (فقط در مسیر حذف مازاد؛ نه در مسیر کمبود) |

### چرا برای سیستم آینده ناکافی است

- **هیچ گرادیانی بین Locked و Flexible نیست.** مفهوم «Strongly Protected / High / Medium / Flexible» قابل بیان نیست؛ فقط یک بیت Locked داریم.
- **دو سیستم قفل موازی و ناسازگار** (person.locked در برابر lockedRows) که رفتار متفاوتی در دو تابع اصلی Solver دارند.
- **قفل نفر در خود Solver اعمال نمی‌شود**؛ فقط بعداً merge/reconcile «برمی‌گرداند». در نتیجه وقتی چند ردیف قفل شوند، Solver ابتدا برای همه (شامل قفل‌ها) برنامه می‌چیند، سپس merge آن را بازنویسی می‌کند، سپس reconcile کمبود/مازادِ به‌وجودآمده را جبران می‌کند. این یعنی **هیچ تضمینی نیست که برنامهٔ بقیه بعد از قفل شدن چند نفر واقعاً بهینه یا حتی سازگار بماند** — ممکن است به بن‌بست جزئی برسد که فقط به‌صورت هشدار ظاهر می‌شود.
- **هیچ تستی برای «چند ردیف قفل‌شده + برنامهٔ بقیه» وجود ندارد** (بخش ۱۲).
- برای هدف «هر فرد درجهٔ آزادی متفاوت دارد»، معماری فعلی فقط با ترکیب‌های ad-hoc (position + workRoutine + locked) می‌تواند تقریب بزند؛ **فیلد صریح Flexibility وجود ندارد (C).**

---

## 10. CONFLICT RESOLUTION ANALYSIS

| تعارض | اولویت فعلی (از کد) | صریح یا تصادفی از ترتیب کد؟ | هزینهٔ نقض قابل‌اندازه‌گیری؟ | بهترین گزینه یا اولین قابل‌قبول؟ |
|---|---|---|---|---|
| Request شخص A در برابر Staffing Need | **Staffing Need برنده است** (درخواست shift «نرم» فقط ممنوعیت در فیلتر می‌سازد و در مسیر اضطراری نادیده گرفته می‌شود)؛ اما Hard OFF/leave در برابر coverage حفظ می‌شود | از ترتیب کد (فیلتر/forceAvailable) | خیر | اولین قابل‌قبول |
| Rest Preference در برابر Productivity | **هیچ Rest Preference صریحی نیست**؛ «استراحت» فقط از سقف متوالی مشتق می‌شود. Productivity (tiebreaker) بعد از penalty و قبل از کسری موظفی اعمال می‌شود | از ترتیب کد | خیر | اولین قابل‌قبول |
| Fixed Pattern در برابر Coverage | **Pattern اول اعمال می‌شود** (پیش‌پردازش) و در fill «صاحب روتین» اگر امروز مطابق الگو نباشد جریمهٔ «خارج از روتین» می‌گیرد؛ ولی در بن‌بست، Coverage می‌تواند pattern را بشکند | نیمه‌صریح (کامنت‌ها) ولی پیاده‌سازی پراکنده | خیر (فقط count خارج‌ازروتین ≤ ۲) | اولین قابل‌قبول |
| Locked Staff در برابر Feasibility کل | Lock همیشه حفظ می‌شود (merge/reconcile)؛ نتیجه‌اش ممکن است Coverage Shortage شود که فقط **هشدار** است | صریح در merge/reconcile، ولی اثرش (بن‌بست) صریح نیست | خیر | اولین قابل‌قبول (و بدون ارزیابی امکان‌پذیری) |
| Hard OFF در برابر reconcile(کمبود) | **reconcile ممکن است Hard OFF را بشکند** و فقط «Mismatched Request» غیربحرانی بسازد | تصادفی از ترتیب کد (نقص فیلتر) | خیر | — |

**نتیجه:** هیچ «Conflict Resolver» واحدی وجود ندارد. اولویت‌ها در فیلترها و comparatorهای پراکندهٔ ۴–۵ مسیر مختلف تعریف شده‌اند و بین آن مسیرها هم یکدست نیستند (مثلاً Hard OFF در solver سخت است، در reconcile نرم).

---

## 11. FALLBACK ANALYSIS

سؤال‌های کلیدی و پاسخ واقعی:

| پرسش | پاسخ فعلی | شواهد |
|---|---|---|
| آیا Constraintها را به ترتیب منطقی Relax می‌کند؟ | **خیر** | Relax فقط «Soft OFF در آخر» و «ناسازگاری روتین در آخر» است. هیچ زنجیرهٔ عمومی Relax نیست. |
| یا صرفاً چند fallback اجرا می‌کند؟ | **بله، چند fallback جدا** | (۱) forceAvailable در solver، (۲) reconcile، (۳) solveWithPriority، (۴) repairCriticalAlerts در generator. |
| می‌داند کدام Preference کم‌ارزش‌تر است؟ | **فقط دو مورد مشخص:** Soft OFF < بقیه، ناسازگاری روتین < بقیه. بقیه ترجیح‌ها هیچ ترتیب نسبی ندارند. | `forceAvailable.sort`، `priority3SoftOff` |
| ممکن است بی‌دلیل قفل کند؟ | **ممکن است به بن‌بست جزئی برسد** که فقط هشدار است (بدون تلاش برای جابه‌جایی بین روزها). چون الگوریتم یک‌بار از ابتدا تا انتها جلو می‌رود و بازنویسی نمی‌کند. | ساختار greedy و loss‌های ترمیمی محدود |
| برنامهٔ ناقص تولید می‌کند؟ | **بله** — gap باقی‌مانده = سلول OFF + هشدار Coverage Shortage. | forceAvailable بعد از اتمام؛ verify |
| Post-processing ممکن است تصمیم Solver را خراب کند؟ | **بله (اثبات‌شده)** — reconcile و ترمیم‌ها و useEffect پیوسته بعد از Solver اجرا و سلول‌ها را تغییر می‌دهند. | بخش ۴.۳ و ۴.۷ |

**نکتهٔ مهم:** در معماری مبنامحور، اگر نامزدی بعد از ۲۴ گام تعمیر هنوز سطح A داشته باشد، کل نامزد حذف می‌شود و اگر هیچ نامزدی نماند، خروجی **۰ سناریو** است (نه «بهترین برنامهٔ ممکن»). این با هدف محصول («در صورت عدم امکان ایده‌آل، بهترین برنامهٔ ممکن را بده») در تضاد است: سیستم به‌جای ارائهٔ بهترین برنامهٔ همراه‌با‌توضیح، **اصلاً برنامه‌ای نمی‌دهد**.

---

## 12. TEST GAP ANALYSIS

### وضعیت اجرای تست‌ها (نکتهٔ محیطی — NEEDS VERIFICATION)

- `node_modules` در این محیط نصب نیست؛ بنابراین تست‌های وابسته به `jalaali-js` (که از طریق `lib/jalali.ts` همهٔ تست‌های Solver را وابسته می‌کند) **در این محیط قابل اجرا نبودند** (خطای `MODULE_NOT_FOUND: jalaali-js`). نتیجهٔ آزمون‌های این گزارش از **کد تست‌ها** است، نه از اجرای آن‌ها. برای تأیید واقعی باید `npm install` شود.
- `package.json`: `"test": "tsx --test tests/*.test.ts"` → **فقط ۱۹ فایل سطح بالا را اجرا می‌کند؛ ۱۰ فایل `tests/domain/*.test.ts` (۱۶۵ تست) خارج از گلوب هستند و با `npm test` اجرا نمی‌شوند.** این خودش یک Gap است.

### چه چیزهایی تست شده (A)

- قوانین سقف ۵ متوالی، وزن شب=۲، استراحت اجباری، شیفت تک‌تک، تگ روتین، مرخصی تعطیل (smart-rules: ~۲۵ تست).
- جبران پوشش و قفل ردیف و جداسازی گروه‌ها (staffing-coverage: ۵ تست).
- تابع هدف مبنامحور، سطح A، تمایز سناریو (scenario-objective + scenario-baseline + scenario-scoring).
- عملیات merge/normalize/cell و guardهای قفل (schedule-operations، shift-edit-guards).

### چه چیزهایی تست نشده (C)

| سناریو | وضعیت |
|---|---|
| کیفیت واقعی برنامه (رضایت، عدالت، استراحت) به‌عنوان assertion | **C** — تست‌ها فقط «قانون رد نشد» را می‌سنجند. |
| Request Satisfaction (به‌جز امتیاز خام در scenario-scoring) | **C** |
| Rest Preference / استراحت منطقی | **C** |
| Locked/Partially-Locked behaviour (چند ردیف قفل → برنامهٔ بقیه) | **C** |
| بن‌بست (Coverage Shortage تحت کمبود نیرو) و رفتار Fallback | **C** |
| Productivity در برابر Request (رقابت) | **C** |
| Post-processing بعد از Solver Hard Constraint را نمی‌شکند | **C** |
| سناریوهای واقعی بیمارستانی (بخش واقعی، سرپرستار/استاف/سرباز/انعطاف‌پذیر) | **C** |
| `solveNursingSchedule` end-to-end (ساخت کامل برنامه) | **B** — فقط به‌صورت جانبی در ساخت baseline استفاده می‌شود؛ تست مستقیم کیفیت ندارد. |
| تست‌های `tests/domain/` در CI | **C** — خارج از گلوب `npm test`. |

### یک یافتهٔ جهت‌دار

تست `tests/scenario-baseline.test.ts` صریحاً assert می‌کند `baselineSimilarityPercent >= 60`. یعنی **رفتار «قفس مبنا» نه‌تنها تست نشده، بلکه به‌عنوان رفتار مطلوب در تست قفل شده است.** این تست پس از تغییر فلسفه باید بازنویسی شود.

---

## 13. FILE-BY-FILE MAP

| فایل | چه می‌کند | وابستگی به Solver اصلی | وضعیت پیشنهادی |
|---|---|---|---|
| `lib/solver.ts` (۱۹۴۹) | موتور اصلی: ساخت برنامه، verify، گزارش پرسنل، محاسبه بهره‌وری/ساعت، solveWithPriority | هستهٔ همه‌چیز | **اصلاح عمده**: استخراج Objective/اولویت‌ها از comparatorها؛ تعریف Hard/Soft ساخت‌یافته |
| `lib/scoring.ts` (۶۷۵) | امتیازدهی وزنی + رضایت درخواست + عدالت + بهینه‌سازی + طبقه‌بندی هشدار | متوسط (verify/گزارش‌ها) | **اصلاح/ادغام**: با objective.ts یک‌دست شود؛ بخش مرده حذف |
| `domain/scenarios/objective.ts` (۳۴۶) | تابع هدف مبنامحور + سطح A + رتبه‌بندی | بالا (مصرف‌کنندهٔ verify) | **اصلاح**: شباهت به مبنا باید از رتبهٔ ۱ به نرم کم‌وزن تنزل کند؛ Request/Fairness/Rest وارد شود |
| `lib/scenarioGenerator.ts` (۶۶۹) | موتور سناریوی مبنامحور، تعمیر regex، فیلتر کیفیت، انتخاب ۳تایی | بالا | **اصلاح**: حذف regex-parsing، افزودن Relax منطقی، خروجی «بهترین ممکن» به‌جای ۰ |
| `lib/balanceChecker.ts` (۲۷۹) | جایگزینی خودکار + قانون آف پیش‌فرض | صفر (مرده) | **حذف یا احیای هدفمند** (قانون آف پیش‌فرض برای فلسفهٔ محصول لازم است) |
| `lib/smartSuggestion.ts` (۱۰۱) | پیشنهاد هوشمند از روی هشدار | کم | **اصلاح یا حذف** |
| `lib/scoring-stub.ts` (۶) | stub مرده | صفر | **حذف** |
| `domain/scheduling/smart-rules.ts` (۴۴۷) | سقف متوالی، شیفت تک، روتین، مرخصی تعطیل | کم (خالص) | **حفظ** (بهترین بخش) |
| `domain/scheduling/staffing-coverage.ts` (۲۹۶) | جبران پوشش دقیق | بالا (بعد از Solver) | **اصلاح**: رعایت Hard OFF/درخواست در مسیر کمبود؛ سازگاری با solver |
| `domain/scheduling/schedule-operations.ts` (۲۱۹) | merge/normalize/updateCell | کم | **حفظ** |
| `domain/scheduling/types.ts` (۱۱۸) | قراردادهای Facade | کم | **حفظ** (+افزودن Flexibility در آینده) |
| `domain/scheduling/alert-lifecycle.ts` | چرخهٔ هشدارهای نادیده‌گرفته | کم | **حفظ** |
| `domain/guards/shift-edit-guards.ts` (۱۱۹) | predicateهای قفل | کم | **حفظ** (ولی تعریف قفل باید ارتقا یابد) |
| `domain/requests/request-scope-matcher.ts` (۱۲۱) | تطبیق دامنهٔ درخواست | کم | **حفظ** و حذف نسخه‌های تکراری |
| `domain/calendar/duty-hours-calculator.ts` | ساعت موظفی | صفر | **حفظ** |
| `domain/logging/solver-report.ts` | گزارش اجرای Solver | کم | **حفظ** |
| `features/scheduling/facades/shift-write-facade.ts` (۴۱۲) | ارکستراسیون optimizer/edit + persist | بالا | **اصلاح**: reconcile/merge داخل آن باید با قواعد واحد شود |
| `features/scheduling/hooks/useScheduleState.ts` (۱۱۱) | state قفل‌ها و برنامه | کم | **حفظ** |
| `features/scheduling/components/ScenarioWorkspace.tsx` (۷۰۶) | UI سناریو/رأی‌گیری | متوسط | **اصلاح** هم‌راستا با تغییر Objective |
| `app/page.tsx` (۹۵۹۵) | ارکستراسیون کل + saveState + handleRunOptimizer + useEffect پیوسته | بالا | **اصلاح**: جداسازی کامل منطق Solver از UI؛ حذف useEffect خودکار reconcile یا تبدیل به عمد |
| `lib/types.ts` / `domain/types/index.ts` | تایپ‌های مشترک | کم | **حفظ** (+فیلد Flexibility/اولویت در آینده) |

---

## 14. REFACTOR PLAN (فقط ترتیب — بدون کد)

**فاز ۰ — تثبیت و ایمنی (پیش‌نیاز هر چیز)**
1. `npm install` و سبز کردن CI واقعی + اصلاح گلوب تست تا `tests/domain/` را هم شامل شود.
2. افزودن تست‌های regression برای رفتار فعلی `solveNursingSchedule` (به‌عنوان baseline رفتاری) پیش از هر تغییر.

**فاز ۱ — یک‌دست‌سازی مدل داده (بدون تغییر رفتار)**
3. تعریف یک‌جا و ساخت‌یافتهٔ Constraint/Preference با `hard`/`soft` + `weight` + `reason` (حذف طبقه‌بندی مبتنی بر پیشوند رشته).
4. حذف `person.locked` یا ادغام کامل با `lockedRows` (یک منبع حقیقت).
5. افزودن فیلد `flexibility` / `lockLevel` (CompletelyLocked…Flexible) به Personnel — فقط داده، بدون تغییر منطق.

**فاز ۲ — استخراج Objective واقعی**
6. استخراج تمام comparatorهای solver.ts به یک تابع امتیاز واحد قابل‌تنظیم.
7. بازنویسی `domain/scenarios/objective.ts`: ترتیب جدید (Hard → Lock → Coverage → Rest → Request → Productivity → Fairness → Baseline به‌عنوان نرم کم‌وزن).
8. حذف/ادغام `scoring.ts` و `objective.ts` و stub.

**فاز ۳ — اصلاح Fallback و Post-processing**
9. اصلاح `reconcileStaffingCoverage` تا در مسیر کمبود، Hard OFF و درخواست‌های shift را رعایت کند (و ناسازگاری‌ها را ساخت‌یافته گزارش دهد).
10. حذف regex-parsing در `repairCriticalAlerts` و جایگزینی با ساختار دادهٔ هشدار.
11. تبدیل خروجی ۰ سناریو به «بهترین برنامهٔ ممکن + گزارش فشار محدودیت‌ها».
12. حذف/محدودسازی useEffect خودکار reconcile (تبدیل به عملیات صریح).

**فاز ۴ — ارتقای Solver به جستجو (اختیاری/بلندمدت)**
13. در صورت نیاز به «بهترین» به‌جای «اولین قابل‌قبول»، جایگزینی سازندهٔ حریصانه با جستجوی محلی/Local Search یا CP-SAT با همان مدل قواعد فاز ۱.
14. اتصال قانون بهره‌وری به Objective با وزن واقعی و رقابت شفاف با Request.

**فاز ۵ — تست کیفیت**
15. تست‌های سناریوی واقعی بیمارستانی، بن‌بست، قفل‌چندردیفه، رضایت درخواست، و عدم‌نقض Hard بعد از Post-processing.

---

## 15. RISKS (بزرگ‌ترین ریسک‌های تغییر Solver)

1. **ریسک رگرسیون رفتاری نامرئی:** چون کیفیت در comparatorهای پراکنده است، هر «بهبود کوچک» می‌تواند خروجی کل ماه را تغییر دهد و بدون تست کیفیت، این تغییر قابل تشخیص نیست.
2. **ریسک شکستن قوانین حقوقی/قانونی:** سقف متوالی، ساعت موظفی، سنوات، بهره‌وری و مرخصی تعطیل به هم گره خورده‌اند؛ دست‌زدن به یکی بدون مدل یکپارچه، ممکن است پرداخت/اضافه‌کار را خراب کند.
3. **ریسک reconcile پس‌اَنداز:** تا وقتی reconcile بعد از Solver سلول‌ها را تغییر می‌دهد، هر Solver جدیدی هم در معرض بازنویسی است. اصلاح Solver بدون اصلاح reconcile بی‌فایده است.
4. **ریسک بن‌بست با قفل‌های زیاد:** معماری فعلی بن‌بست را به‌صورت هشدار می‌پذیرد؛ افزودن Flexibility بدون مدل امکان‌پذیری، فقط پیچیدگی را زیاد می‌کند.
5. **ریسک دادهٔ ذخیره‌شدهٔ قدیمی:** سناریوهای ذخیره‌شده بدون `metrics` از مسیر legacy hydrate می‌گذرند؛ تغییر Objective باید با دادهٔ قدیمی سازگار بماند یا migration داشته باشد.
6. **ریسک UI وابسته به متن هشدار:** خیلی از رفتار UI (کلیک روی هشدار، پیشنهاد، تعمیر) به رشتهٔ فارسی هشدار وابسته است؛ تغییر wording در Solver می‌تواند این‌ها را بی‌صدا بشکند.
7. **ریسک چرخهٔ اثر ذخیره‌سازی:** useEffect پیوسته (reconcile→verify→save→sync→re-render) می‌تواند حلقهٔ تغییر مکرر بسازد؛ تغییر در Solver باید این چرخه را هم در نظر بگیرد.
8. **ریسک تست‌های «قفس‌محور»:** تست `scenario-baseline` رفتار فعلی را قفل کرده است؛ هر تغییر فلسفه باید این تست را همراه بازنویسی کند، وگرنه CI قرمز می‌شود.
9. **ریسک اجرا در مرورگر:** کل Solver + تولید سناریو در کلاینت اجرا می‌شود؛ یک Solver جستجومحور سنگین‌تر باید در Web Worker/سرور اجرا شود وگرنه UI قفل می‌شود.

---

## خلاصهٔ سه‌یافتهٔ محوری

1. **Baseline از نقطهٔ شروع به قفس تبدیل شده است** — معیار رتبه‌بندی فعال، شباهت به مبناست و Request Satisfaction به tiebreaker رتبهٔ ۳ تنزل یافته؛ Fairness عملاً حذف شده.
2. **هیچ Objective صریح و هیچ هزینهٔ قابل‌مقایسه‌ای وجود ندارد** — تصمیم‌گیری در comparatorهای پراکنده با اعداد جادویی است و بین مسیرهای مختلف (solver / solveWithPriority / reconcile) یکدست نیست.
3. **Post-processing می‌تواند تصمیم Solver را بشکند** — `reconcileStaffingCoverage` در مسیر کمبود، Hard OFF و درخواست‌ها را نادیده می‌گیرد و چون «Mismatched Request» غیربحرانی طبقه‌بندی شده، این شکست حتی به‌عنوان خطای سخت هم نمایان نمی‌شود.

> `UNKNOWN / NEEDS VERIFICATION`:
> - اجرای واقعی تست‌های Solver (به‌دلیل نبود `node_modules` در این محیط).
> - دادهٔ واقعی بخش‌ها در Production (تعداد نفرات و قفل‌ها) برای سنجش میزان واقعی بن‌بست.
